import { motion } from 'framer-motion';export default function FaultCard({fault}){const severityColors={Critical:'bg-red-600 text-white',Major:'bg-orange-500 text-white',Minor:'bg-green-500 text-white'};return (<motion.div whileHover={{scale:1.03}} className="bg-white rounded-2xl shadow-lg overflow-hidden"><div className="h-44 w-full overflow-hidden">{fault.image&&<img src={fault.image} alt={fault.fault} className="w-full h-full object-cover"/>}</div><div className="p-4"><div className="flex items-center justify-between"><h3 className="text-lg font-bold">{fault.fault}</h3><span className={`px-2 py-1 rounded-full text-xs ${severityColors[fault.severity]||'bg-gray-200'}`}>{fault.severity}</span></div><p className="text-sm text-gray-600 mt-2"><strong>Machine:</strong> {fault.machine} • <strong>System:</strong> {fault.system}</p><div className="mt-3 text-sm"><p><strong>Symptoms:</strong> {fault.symptoms}</p><p className="mt-2"><strong>Cause:</strong> {fault.cause}</p><p className="mt-2"><strong>Repair:</strong> {fault.repair}</p></div></div></motion.div>);}