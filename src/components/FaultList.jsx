import FaultCard from './FaultCard.jsx';export default function FaultList({faults,query}){const filtered=faults.filter(f=>!query||f.fault.toLowerCase().includes(query.toLowerCase())||f.machine.toLowerCase().includes(query.toLowerCase())||f.system.toLowerCase().includes(query.toLowerCase()));return (<div className="mt-6 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">{filtered.length?filtered.map((f,i)=><FaultCard key={i} fault={f} />):<p className="text-gray-500">No results found.</p>}</div>);}